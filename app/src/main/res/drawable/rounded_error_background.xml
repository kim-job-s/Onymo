<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 모서리 라운드 처리 -->
    <corners android:radius="8dp" />

    <!-- 배경 색상 -->
    <solid android:color="@color/errorBackgroundColor" />

    <!-- 테두리 -->
    <stroke
        android:width="1dp"
        android:color="@color/errorBorderColor" />
</shape>

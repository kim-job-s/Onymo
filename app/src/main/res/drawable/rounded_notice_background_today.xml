<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 바깥쪽 그림자 -->
    <item android:left="6dp" android:top="6dp" android:right="6dp" android:bottom="6dp">
        <shape>
            <gradient
                android:startColor="#33000000"
                android:endColor="#00000000"
                android:angle="90" />
            <corners android:radius="50dp" /> <!-- 기존 둥근 모서리 값 유지 -->
        </shape>
    </item>

    <!-- 내부 그림자 (가장자리 음영) -->
    <item>
        <shape>
            <gradient
                android:startColor="#F0F0F0"
                android:endColor="#FFFFFF"
                android:angle="90" />
            <corners android:radius="50dp" /> <!-- 기존 둥근 모서리 값 유지 -->
        </shape>
    </item>

    <!-- 배경과 테두리 -->
    <item>
        <shape>
            <solid android:color="@android:color/white" /> <!-- 배경 흰색 -->
            <corners android:radius="50dp" /> <!-- 둥근 모서리 -->
            <stroke
                android:width="1dp"
                android:color="@color/colorPrimary" /> <!-- 테두리 파란색 -->
            <padding
                android:left="30dp"
                android:top="15dp"
                android:right="30dp"
                android:bottom="15dp" /> <!-- 내부 패딩 -->
        </shape>
    </item>

</layer-list>
